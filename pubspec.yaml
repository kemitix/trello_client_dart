name: trello_sdk
description: CLI and SDK for accessing the Trello API
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.2 <3.0.0'

dependencies:
  dio: ^4.0.6
  meta: ^1.7.0
  tabular: ^0.6.3+1
  progress_bar: ^1.0.0
  dartz: ^0.10.1

dev_dependencies:
  lints: ^1.0.1
  test: ^1.21.1
  build_runner: ^2.1.10

executables:
  trello: trello
